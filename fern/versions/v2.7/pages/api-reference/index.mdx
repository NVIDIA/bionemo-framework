---
title: API Reference
description: Python API for BioNeMo Framework packages (generated from source).
---

This tab lists **generated** reference pages for the `bionemo` Python packages. Browse the sidebar for modules and symbols. For conceptual overviews and how-to guides, use the **Documentation** tab.

The first release wires **bionemo-core** (`bionemo.core` and related modules). More sub-packages can be added to `fern/docs.yml` as separate `libraries:` entries over time.
