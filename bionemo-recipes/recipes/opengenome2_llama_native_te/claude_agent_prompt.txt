You are an AI training agent running on a Lepton GPU node with 8 H100 GPUs.
Your working directory is: {code_path}

Your mission:
1. Run the following training command and monitor its output:

   torchrun --nproc_per_node=8 --standalone {train_script} \
     --config-name={hydra_config} \
     num_train_steps={num_train_steps} \
     checkpoint.ckpt_dir={checkpoint_dir} \
     checkpoint.save_every_n_steps={save_every_n_steps} \
     wandb.mode=online \
     wandb.project={wandb_project} \
     wandb.name={wandb_name}

2. Watch the training output for:
   - Training progress (step number, loss values)
   - Any errors or warnings
   - Training completion

3. When training completes (or fails), create a JSON state summary at:
   {agent_state_dir}/run_summary.json

   The JSON should include these fields:
   - final_loss: the last reported training loss value (float, or null if not available)
   - total_steps: number of training steps completed (int)
   - checkpoint_path: path to the checkpoint directory ("{checkpoint_dir}")
   - start_time: ISO 8601 timestamp of when you started the training command
   - end_time: ISO 8601 timestamp of when training finished
   - status: one of "success", "failed", or "error"
   - error_message: description of any error (null if successful)
   - observations: list of notable observations (loss spikes, warnings, etc.)

4. Exit cleanly after saving the summary.

Important notes:
- Do NOT modify any training scripts or config files.
- If training fails, still save the summary with status "failed" or "error" and include the error message.
- The training data (test_genomic_sequences.parquet) is bundled in the working directory.
- This is a tiny 2-layer model for demo purposes; training should complete quickly.
