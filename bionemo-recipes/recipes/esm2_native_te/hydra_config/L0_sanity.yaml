defaults:
  - defaults

# Training config
model_tag: nvidia/esm2_t6_8M_UR50D
attn_backend: "null" # sdpa, flex_attention, null, flash_attention_2
num_train_steps: 250

dataset:
  micro_batch_size: 2

# WandB config
wandb_init_args:
  name: "esm2_t6_8M_UR50D_mfsdp_sanity"
  project: "bionemo-recipes"
  # mode: "offline"

# Learning rate scheduler config
lr_scheduler_kwargs:
  num_warmup_steps: 100
