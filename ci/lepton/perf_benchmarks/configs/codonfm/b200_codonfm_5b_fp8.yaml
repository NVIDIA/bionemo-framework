# @package _global_
# AUTO-GENERATED by ci/benchmarks/generate_benchmark_configs.py
# Source row: B200, CodonFM, encodon_5b, FP8
# Regenerate: python ci/benchmarks/generate_benchmark_configs.py
# NOTE: Tentative CP: port over from ESM2 recipe
defaults:
  - /base
  - _self_

node_group: TBD-b200-node-group
mount_from: TBD-b200-mount
device_type: gpu
gpu_type: b200
recipe_subdir: codonfm_native_te
model_type: codonfm
variant: train
framework: native
precision: fp8
te_enabled: true
fp8_enabled: true
fp8_recipe: transformer_engine.common.recipe.Float8BlockScaling
fp8_format: E4M3
num_train_steps: 500
micro_batch_size: 4
log_mfu: true
total_gpus: "${multiply:${num_devices},${num_nodes}}"
wandb_init_args:
  project: "perf_benchmarks__recipes__${sanitize:${branch}}"
  group: "${model_type}__${task_cmd}__${total_gpus}gpus__${sanitize:${gpu_type}}"
  job_type: "${recipe_subdir}"
  name: null
products:
- config: encodon_5b
  task_cmd: train_fsdp2
  num_nodes: 1
  num_devices: 8
  cp_size: 1
  resource_shape: gpu.8xb200
  wandb_name: "bench__codonfm_5b__fp8__1n8g_cp1__${now:%Y%m%d-%H%M%S}__${gitsha:}"
  job_name: bench-b200-codonfm-5b-fp8-1n8g_cp1
- config: encodon_5b
  task_cmd: train_fsdp2_cp
  num_nodes: 1
  num_devices: 8
  cp_size: 4
  resource_shape: gpu.8xb200
  wandb_name: "bench__codonfm_5b__fp8__1n8g_cp4__${now:%Y%m%d-%H%M%S}__${gitsha:}"
  job_name: bench-b200-codonfm-5b-fp8-1n8g_cp4
  disabled: true
- config: encodon_5b
  task_cmd: train_fsdp2_cp
  num_nodes: 1
  num_devices: 8
  cp_size: 8
  resource_shape: gpu.8xb200
  wandb_name: "bench__codonfm_5b__fp8__1n8g_cp8__${now:%Y%m%d-%H%M%S}__${gitsha:}"
  job_name: bench-b200-codonfm-5b-fp8-1n8g_cp8
  disabled: true
run_script: |
  wget -O init.sh https://raw.githubusercontent.com/leptonai/scripts/main/lepton_env_to_pytorch.sh;
  chmod +x init.sh;
  source init.sh;

  HYDRA_FULL_ERROR=1 torchrun \
    --nnodes=$NNODES \
    --nproc_per_node=$(nvidia-smi --query-gpu=gpu_name --format=csv,noheader | wc -l) \
    --node_rank=$NODE_RANK \
    --master_addr=$MASTER_ADDR \
    --master_port=$MASTER_PORT \
    ${task_cmd}.py \
    --config-name ${config}.yaml \
    +wandb_init_args.mode=${wandb_init_args.mode} \
    wandb_init_args.project=${wandb_init_args.project} \
    +wandb_init_args.group=${wandb_init_args.group} \
    +wandb_init_args.job_type=${wandb_init_args.job_type} \
    wandb_init_args.name=${wandb_name} \
    num_train_steps=${num_train_steps} \
    dataset.micro_batch_size=${micro_batch_size} \
    log_mfu=true \
    checkpoint.ckpt_dir= \
    checkpoint.save_final_model=false \
    checkpoint.resume_from_checkpoint=false \
    fp8_config.enabled=${fp8_enabled}
expected_output:
  description: Benchmark validation for B200 CodonFM encodon_5b FP8
  tests:
  - criteria:
      exit_code: 0
      metrics:
        train/step_time:
          operator: gt
          value: 0
        train/tokens_per_second_per_gpu:
          operator: gt
          value: 0
        train/mfu_percent:
          operator: geq
          value: 0
        train/tflops_per_gpu:
          operator: geq
          value: 0
        train/gpu_memory_allocated_max_gb:
          operator: gt
          value: 0
